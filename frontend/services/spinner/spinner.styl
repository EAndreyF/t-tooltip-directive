.spinner {
  width: 100%;
  height: 100%;

  img {
    margin: 0 auto;
    display: block;
    top: 50%;
    position: relative;
    margin-top: -25px;
    width: 50px;
  }
}

.spinner-background {
  background: none !important;
}
